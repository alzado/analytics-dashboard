# BigQuery Configuration (REQUIRED)
# Set these environment variables to connect to your BigQuery data source

# GCP Project ID
BIGQUERY_PROJECT_ID=your-gcp-project-id

# BigQuery Dataset name
BIGQUERY_DATASET=your-dataset-name

# BigQuery Table name
BIGQUERY_TABLE=your-table-name

# Optional: Path to service account credentials JSON file
# If not provided, will use default GCP credentials (gcloud auth)
BIGQUERY_CREDENTIALS_PATH=/app/credentials/service-account.json

# IMPORTANT: Copy this file to .env and update with your actual values:
# cp .env.example .env
